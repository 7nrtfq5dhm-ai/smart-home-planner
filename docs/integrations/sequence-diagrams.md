---
id: sequence-diagrams
title: Sequence-диаграммы
sidebar_label: Sequence-диаграммы
---

# Sequence-диаграммы

## SD-01 — Регистрация пользователя

```
Пользователь       Фронтенд          Backend API        Email/SMS         PostgreSQL
      │                │                   │                  │                │
      │──нажал «Рег.»─►│                   │                  │                │
      │                │──POST /auth/──────►│                  │                │
      │                │   send-otp        │                  │                │
      │                │                   │──отправить OTP──►│                │
      │                │                   │                  │──OTP на почту─►│
      │◄───────────────│                   │                  │                │
      │   форма OTP    │                   │                  │                │
      │──ввёл OTP─────►│                   │                  │                │
      │                │──POST /auth/──────►│                  │                │
      │                │   verify-otp      │──проверка────────────────────────►│
      │                │                   │◄─────────────────────────────────│
      │                │                   │   OK                              │
      │──ввёл пароль──►│                   │                  │                │
      │                │──POST /auth/──────►│                  │                │
      │                │   register        │──INSERT user─────────────────────►│
      │                │                   │◄─────────────────────────────────│
      │                │◄──JWT-токен───────│                  │                │
      │◄──главный экран│                   │                  │                │
```

---

## SD-02 — Создание проекта через AI (асинхронно)

```
Пользователь    Фронтенд       Backend API      RabbitMQ      AI-микросервис    PostgreSQL
      │              │               │               │                │               │
      │──промпт─────►│               │               │                │               │
      │              │──POST /──────►│               │                │               │
      │              │  generate     │──INSERT────────────────────────────────────────►│
      │              │               │  ai_task                       │               │
      │              │               │──publish──────►│               │               │
      │              │               │  task          │               │               │
      │              │◄──201 Created─│               │                │               │
      │◄─«Генерируем»│               │               │                │               │
      │              │               │               │──consume task─►│               │
      │              │               │               │                │──AI генерация │
      │              │               │               │                │──UPDATE──────►│
      │              │               │               │                │  project      │
      │              │               │◄──webhook DONE│────────────────│               │
      │              │◄──статус OK───│               │                │               │
      │◄──редактор───│               │               │                │               │
```

---

## SD-03 — AI-команда в редакторе (синхронно)

```
Пользователь    Фронтенд       Backend API      AI-сервис
      │              │               │               │
      │──команда────►│               │               │
      │   «Кухню     │               │               │
      │   больше»    │               │               │
      │              │──POST /───────►│               │
      │              │  ai-commands  │──интерпрет.──►│
      │              │               │◄──изменения───│
      │              │               │──UPDATE──────►│ (PostgreSQL)
      │              │◄──200 OK──────│               │
      │              │   {project}   │               │
      │              │──перерисовка  │               │
      │◄─«Применено»─│   холста      │               │
```
