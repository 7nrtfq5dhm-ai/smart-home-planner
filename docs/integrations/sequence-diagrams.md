---
id: sequence-diagrams
title: Sequence-диаграммы
sidebar_label: Sequence-диаграммы
---

# Sequence-диаграммы

## SD-01 — Регистрация пользователя (UC-01)

```
Пользователь    Фронтенд        Backend API      Email/SMS       PostgreSQL
      │               │                │               │               │
      │──«Регистр.»──►│               │               │               │
      │               │──POST /send-otp►              │               │
      │               │                │──отправить───►│               │
      │               │                │               │──OTP──►user   │
      │◄──форма OTP───│               │               │               │
      │──ввёл OTP────►│               │               │               │
      │               │──POST /verify──►              │               │
      │               │                │──проверка─────────────────────►
      │               │                │◄──OK──────────────────────────│
      │──ввёл пароль─►│               │               │               │
      │               │──POST /register►              │               │
      │               │                │──INSERT user──────────────────►
      │               │                │◄──OK──────────────────────────│
      │               │◄──JWT-токен────│               │               │
      │◄──главный экран│              │               │               │
```

---

## SD-02 — Создание проекта через AI (асинхронно)

```
Пользователь   Фронтенд      Backend API     RabbitMQ    AI-сервис   PostgreSQL
      │              │               │              │           │           │
      │──промпт─────►│               │              │           │           │
      │              │──POST /generate►             │           │           │
      │              │               │──INSERT task──────────────────────── ►
      │              │               │──publish──────►           │           │
      │              │◄──201 {taskId}│              │           │           │
      │◄──«Генерируем»│              │              │           │           │
      │              │               │              │──consume──►           │
      │              │               │              │           │──генерация│
      │              │               │              │           │──UPDATE───►
      │              │               │◄──webhook COMPLETED───────│           │
      │              │◄──статус OK───│              │           │           │
      │◄──редактор───│               │              │           │           │
```

---

## SD-03 — AI-команда в редакторе (синхронно)

```
Пользователь   Фронтенд      Backend API    AI-сервис   PostgreSQL
      │              │               │            │           │
      │──команда────►│               │            │           │
      │   «Кухню     │               │            │           │
      │   больше»    │               │            │           │
      │              │──POST /ai-cmd──►            │           │
      │              │               │──интерпр──►│           │
      │              │               │◄──изменения│           │
      │              │               │──UPDATE────────────────►
      │              │◄──200 {project}│            │           │
      │              │──перерисовка   │            │           │
      │◄──«Применено»─│              │            │           │
```
